const Loader = () => {
  return (
    <div style={{minHeight: '100vh', width: '100%', display: 'flex', alignItems: 'center', justifyContent: 'center'}}>
      <img src="/loader.gif" style={{width: 'auto', height: '242px'}} />
    </div>
  )
}

export default Loader
